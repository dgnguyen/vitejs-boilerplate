apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cp10-front-cms.fullname" . }}-test-connection"
  labels:
    {{- include "cp10-front-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cp10-front-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
